--- origsrc/mysql-connector-odbc-5.2.5-src/driver/CMakeLists.txt	2013-04-04 08:53:56.000000000 -0500
+++ src/mysql-connector-odbc-5.2.5-src/driver/CMakeLists.txt	2013-06-03 22:25:32.615400100 -0500
@@ -45,25 +45,25 @@ IF(WIN32)
 	SET(DRIVER_SRCS ${DRIVER_SRCS} driver.def driver.rc catalog.h driver.h error.h myutil.h parse.h)
 ENDIF(WIN32)
 
-IF(APPLE)
+IF(APPLE OR WIN32 OR CYGWIN)
 	ADD_LIBRARY(${DRIVER_NAME} MODULE ${DRIVER_SRCS})
-ELSE(APPLE)
+ELSE(APPLE OR WIN32 OR CYGWIN)
 	ADD_LIBRARY(${DRIVER_NAME} SHARED ${DRIVER_SRCS})
-ENDIF(APPLE)
+ENDIF(APPLE OR WIN32 OR CYGWIN)
 
 INSTALL(TARGETS ${DRIVER_NAME} DESTINATION ${LIB_SUBDIR})
 
-IF(WIN32)
+IF(WIN32 OR CYGWIN)
 	TARGET_LINK_LIBRARIES(${DRIVER_NAME} myodbc3u
 				${MYSQL_CLIENT_LIBS} ${ODBCINSTLIB} ${SECURE32_LIB})
-ELSE(WIN32)
+ELSE(WIN32 OR CYGWIN)
 	SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${ODBC_LINK_FLAGS}")
 	TARGET_LINK_LIBRARIES(${DRIVER_NAME} ${MYSQL_CLIENT_LIBS} ${CMAKE_THREAD_LIBS_INIT} m)
 	TARGET_LINK_LIBRARIES(${DRIVER_NAME} myodbc3u)
 
   INCLUDE_DIRECTORIES(${DL_INCLUDES})
 
-ENDIF(WIN32)
+ENDIF(WIN32 OR CYGWIN)
 
 IF(MYSQL_CXX_LINKAGE)
   SET_TARGET_PROPERTIES(${DRIVER_NAME} PROPERTIES LINKER_LANGUAGE CXX)
--- origsrc/mysql-connector-odbc-5.2.5-src/installer/CMakeLists.txt	2013-04-04 08:53:56.000000000 -0500
+++ src/mysql-connector-odbc-5.2.5-src/installer/CMakeLists.txt	2013-06-03 18:41:39.298058300 -0500
@@ -31,13 +31,13 @@ ENDIF(DL_INCLUDES)
 
 ADD_EXECUTABLE(myodbc-installer myodbc3i.c)
 
-IF(WIN32)
+IF(WIN32 OR CYGWIN)
   TARGET_LINK_LIBRARIES(myodbc-installer myodbc3u ${ODBCLIB} ${ODBCINSTLIB}
       ${MYSQL_CLIENT_LIBS})
-ELSE(WIN32)
+ELSE(WIN32 OR CYGWIN)
   SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${ODBC_LINK_FLAGS}")
   TARGET_LINK_LIBRARIES(myodbc-installer myodbc3u ${MYSQL_CLIENT_LIBS} ${CMAKE_THREAD_LIBS_INIT} m)
-ENDIF(WIN32)
+ENDIF(WIN32 OR CYGWIN)
 
 IF(MYSQL_CXX_LINKAGE)
   SET_TARGET_PROPERTIES(myodbc-installer PROPERTIES LINKER_LANGUAGE CXX)
--- origsrc/mysql-connector-odbc-5.2.5-src/test/CMakeLists.txt	2013-04-04 08:53:56.000000000 -0500
+++ src/mysql-connector-odbc-5.2.5-src/test/CMakeLists.txt	2013-06-03 18:42:33.562162000 -0500
@@ -46,9 +46,9 @@ FOREACH(T my_basics my_blob my_bulk my_c
 
     INSTALL(TARGETS ${T} DESTINATION test COMPONENT tests)
 
-	IF(WIN32)
+	IF(WIN32 OR CYGWIN)
 	  TARGET_LINK_LIBRARIES(${T} ${ODBCLIB} ${ODBCINSTLIB})
-	ENDIF(WIN32)
+	ENDIF(WIN32 OR CYGWIN)
 	
 	ADD_TEST(${T} ${T})
 ENDFOREACH(T)
--- origsrc/mysql-connector-odbc-5.2.5-src/util/CMakeLists.txt	2013-04-04 08:53:56.000000000 -0500
+++ src/mysql-connector-odbc-5.2.5-src/util/CMakeLists.txt	2013-06-03 18:38:37.990688100 -0500
@@ -38,7 +38,7 @@ ADD_LIBRARY(myodbc3u STATIC ${myodbc3u_S
 TARGET_LINK_LIBRARIES(myodbc3u ${ODBCINSTLIB})
 
 IF(NOT WIN32)
-  SET_TARGET_PROPERTIES(myodbc3u PROPERTIES COMPILE_FLAGS ${CMAKE_SHARED_LIBRARY_C_FLAGS})
+  SET_TARGET_PROPERTIES(myodbc3u PROPERTIES COMPILE_FLAGS "${CMAKE_SHARED_LIBRARY_C_FLAGS}")
 
   INCLUDE_DIRECTORIES(${DL_INCLUDES})
 
